import colors from './variables/colors'

export default {
    clickable: {
        border: `1px solid ${colors.edit}`,
        borderRadius: '3px',
        boxShadow: '0 1px 3px rgba(0, 0, 0, .3)',
    }
}
